import org.joda.time.*

private fun toDateTimeRange(timeZone: DateTimeZone): ClosedRange<DateTime> {
  val minOffset = 1L
  val maxOffset = 1L
  val now = DateTime.now(timeZone)
  val min = now.withDurationAdded(minOffset, 1)
  val max = now.withDurationAdded(maxOffset, 1)
  return min..max
}
// change
// change
fun double17625(x: Int) = x * 2
fun double27298(x: Int) = x * 2
fun double16901(x: Int) = x * 2
fun double22711(x: Int) = x * 2
fun double24734(x: Int) = x * 2
fun double22625(x: Int) = x * 2
fun double18794(x: Int) = x * 2
fun double11756(x: Int) = x * 2
fun double17395(x: Int) = x * 2
fun double28964(x: Int) = x * 2
fun double5161(x: Int) = x * 2
fun double14512(x: Int) = x * 2
fun double8337(x: Int) = x * 2
fun double25639(x: Int) = x * 2
fun double260(x: Int) = x * 2
